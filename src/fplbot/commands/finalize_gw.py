"""Post-GW actuals into evaluation tables (Phase 5b)."""


def run(gameweek: int) -> None:
    """Join official scores to stored predictions for calibration."""
    raise NotImplementedError(f"finalize-gw {gameweek}")
