"""Advisory run: models + report + evaluation record (Phase 4+)."""


def run() -> None:
    """Load silver + my_team; emit human report and persist a run."""
    raise NotImplementedError("suggest")
